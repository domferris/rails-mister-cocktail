<div class="cocktails">
  <% @cocktails.each do |cocktail| %>
    <%= link_to cocktail_path(cocktail) do %>
      <div class="cocktail-card" style="background-image: linear-gradient(45deg, rgba(0, 0, 0, 0.3), rgba(0, 0, 0, 0.8)), url('<%= cl_image_path cocktail.photo.key %>')">
        <p><%= cocktail.name %></p>
      </div>
    <% end %>
  <% end %>
</div>
